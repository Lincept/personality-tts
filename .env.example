# ==========================================
# Personality-TTS 配置文件示例
# ==========================================
# 复制此文件为 .env 并填入你的 API 密钥

# ==========================================
# OpenAI 兼容 LLM 配置
# ==========================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# ==========================================
# TTS 服务配置
# ==========================================

# 通义千问 TTS (Qwen3)
QWEN3_API_KEY=your_qwen3_api_key_here

# 火山引擎 Seed2 TTS
VOLCENGINE_APP_ID=your_volcengine_app_id_here
VOLCENGINE_API_KEY=your_volcengine_api_key_here
# 或使用 access_token（二选一）
# VOLCENGINE_ACCESS_TOKEN=your_volcengine_access_token_here

# MiniMax TTS
MINIMAX_API_KEY=your_minimax_api_key_here
MINIMAX_GROUP_ID=your_minimax_group_id_here

# ==========================================
# Mem0 记忆管理配置
# ==========================================

# 是否启用 Mem0 长期记忆功能
ENABLE_MEM0=true

# Mem0 使用的 LLM 配置（用于记忆提取）
# 如果不设置，将使用上面的 OPENAI_API_KEY 和 OPENAI_BASE_URL
MEM0_LLM_API_KEY=your_openai_api_key_here
MEM0_LLM_BASE_URL=https://api.openai.com/v1
MEM0_LLM_MODEL=gpt-4o-mini

# 默认用户 ID（用于区分不同用户的记忆）
MEM0_USER_ID=default_user

# ==========================================
# 使用说明
# ==========================================
# 1. 复制此文件为 .env
# 2. 填入你的 API 密钥
# 3. 运行 pip install -r requirements.txt 安装依赖
# 4. 运行 python src/main.py 启动程序
#
# Mem0 功能说明：
# - 启用后会自动保存对话历史到长期记忆
# - 支持跨会话记住用户信息、偏好、历史对话
# - 使用本地 Qdrant 向量数据库存储（数据保存在 ./data/qdrant）
# - 交互命令：
#   /memories - 查看长期记忆
#   /clearmem - 清除长期记忆
#   /user <用户ID> - 切换用户
