# two_stage (Personality TTS) 依赖
# 核心
openai>=1.0.0
python-dotenv>=1.0.0
tenacity>=8.0.0
pydantic>=2.0.0
PyYAML>=6.0

# DashScope ASR/TTS
dashscope

# Volcengine realtime TTS (websocket)
websockets>=12.0

# 音频与A/V处理
numpy>=1.24.0
pyaudio

# 长期记忆（可选功能，但默认代码路径会导入）
mem0ai
qdrant-client

# 开发/测试
pytest>=7.0.0
pytest-asyncio>=0.21.0
